import React from 'react'
import ProductList from '../ProductList/ProductList'

const NewArrivals = ({selectedTab}) => {
  return (
    <>
      <ProductList selectedTab={selectedTab} />
    </>
  )
}

export default NewArrivals
